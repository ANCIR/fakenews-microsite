{
  "devDependencies": {
    "coveralls": "^3.0.0",
    "jscoverage": "^0.6.0",
    "json-strictify": "^3.0.0",
    "nodeunit": "^0.11.2"
  },
  "scripts": {
    "test": "nodeunit test",
    "coveralls": "jscoverage lib && nodeunit --reporter-lcov test | coveralls"
  }
}
